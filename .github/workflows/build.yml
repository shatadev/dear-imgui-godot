name: build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

env:
  # Keep in sync with imgui/Makefile nightly. Known working
  RUST_NIGHTLY: nightly-2026-06-19

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-19
          components: rust-src

      # rust-cache does not read .cargo/config.toml. point it at the real
      # target-dir (/addons/dear-imgui-godot/target).
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: imgui -> ../addons/dear-imgui-godot/target
          key: native-linux

      - name: Build editor + release
        working-directory: imgui
        run: make native

      - uses: actions/upload-artifact@v4
        with:
          name: lib-linux-x86_64
          if-no-files-found: error
          path: |
            addons/dear-imgui-godot/target/editor/libdear_imgui_godot.so
            addons/dear-imgui-godot/target/release/libdear_imgui_godot.so

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-19
          components: rust-src

      # imgui-sys may run bindgen over cimgui headers; provide libclang so it resolves.
      - uses: KyleMayes/install-llvm-action@v2
        with:
          version: "18"

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: imgui -> ../addons/dear-imgui-godot/target
          key: native-windows

      # Direct cargo (make isn't reliably present on Windows runners). MSVC cl.exe
      # (preinstalled) is found automatically by the cc crate for the imgui C++ build.
      - name: Build editor + release
        working-directory: imgui
        shell: pwsh
        run: |
          cargo +$env:RUST_NIGHTLY build --profile editor --features editor-docs
          cargo +$env:RUST_NIGHTLY build --release

      - uses: actions/upload-artifact@v4
        with:
          name: lib-windows-x86_64
          if-no-files-found: error
          path: |
            addons/dear-imgui-godot/target/editor/dear_imgui_godot.dll
            addons/dear-imgui-godot/target/release/dear_imgui_godot.dll

  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-19
          components: rust-src
          targets: x86_64-apple-darwin,aarch64-apple-darwin

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: imgui -> ../addons/dear-imgui-godot/target
          key: native-macos

      - name: Build universal editor + release
        working-directory: imgui
        shell: bash
        run: |
          set -euo pipefail
          # cargo --target writes to target/<triple>/ only; create the bare dirs lipo writes into.
          mkdir -p ../addons/dear-imgui-godot/target/editor ../addons/dear-imgui-godot/target/release
          # editor (release-optimized + editor-docs)
          cargo +"$RUST_NIGHTLY" build --profile editor --features editor-docs --target x86_64-apple-darwin
          cargo +"$RUST_NIGHTLY" build --profile editor --features editor-docs --target aarch64-apple-darwin
          lipo -create -output ../addons/dear-imgui-godot/target/editor/libdear_imgui_godot.dylib \
            ../addons/dear-imgui-godot/target/x86_64-apple-darwin/editor/libdear_imgui_godot.dylib \
            ../addons/dear-imgui-godot/target/aarch64-apple-darwin/editor/libdear_imgui_godot.dylib
          # release
          cargo +"$RUST_NIGHTLY" build --release --target x86_64-apple-darwin
          cargo +"$RUST_NIGHTLY" build --release --target aarch64-apple-darwin
          lipo -create -output ../addons/dear-imgui-godot/target/release/libdear_imgui_godot.dylib \
            ../addons/dear-imgui-godot/target/x86_64-apple-darwin/release/libdear_imgui_godot.dylib \
            ../addons/dear-imgui-godot/target/aarch64-apple-darwin/release/libdear_imgui_godot.dylib
          lipo -info ../addons/dear-imgui-godot/target/release/libdear_imgui_godot.dylib

      - uses: actions/upload-artifact@v4
        with:
          name: lib-macos-universal
          if-no-files-found: error
          path: |
            addons/dear-imgui-godot/target/editor/libdear_imgui_godot.dylib
            addons/dear-imgui-godot/target/release/libdear_imgui_godot.dylib

  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-06-19
          components: rust-src # required for -Zbuild-std
          targets: wasm32-unknown-emscripten

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: imgui -> ../addons/dear-imgui-godot/target
          key: wasm

      - uses: mymindstorm/setup-emsdk@v14
        with:
          version: 4.0.20
          actions-cache-folder: emsdk-cache-4.0.20

      - name: Sanity check toolchains
        run: emcc -v && em++ -v

      - name: Build release wasm (threaded + nothreads)
        working-directory: imgui
        run: make wasm-release

      - uses: actions/upload-artifact@v4
        with:
          name: lib-wasm
          if-no-files-found: error
          path: |
            addons/dear-imgui-godot/target/wasm32-unknown-emscripten/release/dear_imgui_godot.threads.wasm
            addons/dear-imgui-godot/target/wasm32-unknown-emscripten/release/dear_imgui_godot.wasm

  package:
    needs: [linux, windows, macos, wasm]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: lib-*
          path: dist

      - name: Assemble addon target tree
        run: |
          set -euo pipefail
          base=addons/dear-imgui-godot/target
          mkdir -p "$base/editor" "$base/release" "$base/wasm32-unknown-emscripten/release"
          for job in lib-linux-x86_64 lib-windows-x86_64 lib-macos-universal; do
            cp -v "dist/$job/editor/"*  "$base/editor/"
            cp -v "dist/$job/release/"* "$base/release/"
          done
          cp -v dist/lib-wasm/*.wasm "$base/wasm32-unknown-emscripten/release/"
          ls -R "$base"

      - uses: actions/upload-artifact@v4
        with:
          name: dear-imgui-godot
          if-no-files-found: error
          path: |
            addons/dear-imgui-godot/dear-imgui-godot.gdextension
            addons/dear-imgui-godot/dear-imgui-godot.gdextension.uid
            addons/dear-imgui-godot/plugin.cfg
            addons/dear-imgui-godot/plugin.gd
            addons/dear-imgui-godot/plugin.gd.uid
            addons/dear-imgui-godot/imgui_api.tscn
            addons/dear-imgui-godot/target/editor/libdear_imgui_godot.so
            addons/dear-imgui-godot/target/release/libdear_imgui_godot.so
            addons/dear-imgui-godot/target/editor/dear_imgui_godot.dll
            addons/dear-imgui-godot/target/release/dear_imgui_godot.dll
            addons/dear-imgui-godot/target/editor/libdear_imgui_godot.dylib
            addons/dear-imgui-godot/target/release/libdear_imgui_godot.dylib
            addons/dear-imgui-godot/target/wasm32-unknown-emscripten/release/dear_imgui_godot.threads.wasm
            addons/dear-imgui-godot/target/wasm32-unknown-emscripten/release/dear_imgui_godot.wasm
